<apex:page standardController="Account" recordSetVar="accountList">
    <apex:pageBlock >
        <!-- <apex:pageBlockTable value="{!selected}" var="a">
            <apex:column headerValue="Account Name" value="{!a.Name}" />
            <apex:column headerValue="Industry" value="{!a.Industry}" />
        </apex:pageBlockTable> -->

        <apex:pageBlockTable value="{!selected}" var="a">
            <apex:repeat value="{!$ObjectType.Account.FieldSets.AccountRecordFieldSet}" var="field">
                <apex:column headerValue="{!field.Label}" value="{!a[field]}"/>
            </apex:repeat>

        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>